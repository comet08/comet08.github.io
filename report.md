# 통신 D2C 가입/관리 앱·웹 클라이언트

`React Native 앱` `Next.js 웹` `모노레포` `TypeScript` `React Query` `Zustand`

> 통신 상품을 앱/웹에서 직접 가입·개통·관리하는 D2C 서비스의 클라이언트를 모노레포(앱·웹·웹뷰 + 공유 패키지)로 개발. 화면 구현보다 **번들 최적화, 공유 구조 설계, 웹뷰 인증 디버깅**에 집중.
> 

---

### 번들 사이즈 최적화 — 앱 번들 17.1MB → 9.55MB

- mock 라이브러리(mirage.js·faker.js)가 env 가드에도 **정적 import 탓에 운영 번들에 포함**되던 걸 조건부 `require`로 분리
- RN 기본 번들러 Metro의 **트리쉐이킹 미지원**을 확인하고 lodash를 개별 모듈 import로 전환, ESLint 정렬로 재유입 차단
- `react-native-bundle-visualizer`로 측정 → 원인 진단 → 검증까지. 과정은 사내 FE 위키에 문서화

### 웹뷰 쿠키 인증 디버깅

- 앱 토큰을 웹뷰 쿠키로 넘기는 인증에서 쿠키가 안 먹는 이슈를, **`react-native-cookies`의 Android / iOS 네이티브 구현까지 직접 읽고** 원인 파악
- iOS `WKHTTPCookieStore`의 셋팅 타이밍·재set 동작 차이를 정리, **도메인 분기**로 핫픽스

### 구조 설계

- **모노레포 공유 패키지 분리** — 앱·웹·웹뷰가 복붙하던 코드를 utils/styles/hooks 패키지로 추출, 순환 의존 제거
- **체크아웃 공통 레이아웃** — 여러 결제 플로우를 조합형 템플릿으로 통합, 트랜잭션 래퍼로 중복 결제 차단
- **WebView ↔ Native 메시지 브리지** — 네비게이션을 메시지 프로토콜로 표준화 (멀티스텝 결제용 원자적 화면 교체 포함)

---

*상세 근거·기여 범위 구분은 별도 노트(RBM-클라이언트-개발포폴.md) 참고.*

# 통신 다이렉트 가입/관리 서비스 · 클라이언트 (RN 앱 + Next.js 웹)

> 통신 상품을 앱/웹에서 직접 가입·개통·관리하는 D2C 서비스의 클라이언트를 **모노레포(앱·웹·웹뷰 + 공유 패키지)** 로 개발.
> 
> 
> 단순 화면 구현보다 **번들/성능 최적화, 공유 패키지 구조 설계, 실패 비용이 큰 플로우의 안정성**에 집중했다.
> 
> *(회사·상품·파트너사·내부 식별자는 의도적으로 일반화. 아래 내용은 모두 본인 커밋 기준이며, 공동 작업한 부분은 기여 범위를 명시했다.)*
> 

---

## 기술 스택

| 영역 | 사용 기술 |
| --- | --- |
| 공통 | TypeScript, 모노레포(Yarn Workspaces + Turborepo), 공유 패키지(ui / services / hooks / utils / styles / constants / types) |
| 웹 | Next.js (Pages → App Router 부분 마이그레이션, SSR), Emotion |
| 앱 | React Native, React Navigation, WebView 하이브리드, CodePush(OTA) |
| 상태/데이터 | TanStack React Query(서버 상태·캐시), Zustand(클라이언트 상태·persist) |
| 빌드/분석 | @next/bundle-analyzer, next/dynamic 코드 스플리팅, ESLint import 정렬 |

---

## React Native 번들 사이즈 신경 쓴 부분

기능 개발에 치이다 보면 번들이 조용히 커진다. 번들 사이즈를 한 번 들여다보고, 눈에 띄는 부분을 측정 기반으로 정리했다. (과정은 사내 FE 위키에 문서로 남김)

### 1) 측정부터

- `react-native-bundle-visualizer` / vscode `import-cost`로 **무엇이 번들을 키우는지 먼저 확인**하고 손댔다. 웹은 `@next/bundle-analyzer` 사용.

### 2) dev 전용 mock 라이브러리가 운영 번들에 포함되던 문제

- mock API(mirage.js + faker.js)가 `initMockServer`에서 env 가드를 하는데도, **`mocks/index.ts`가 정적 import라 환경과 무관하게 번들에 같이 들어가던** 게 원인이었다.
- `if (process.env.NODE_ENV === 'development') { require('@api/mocks') }` 형태의 **조건부 require로 바꿔** 운영 빌드에서 mock 의존성(특히 큰 faker.js)이 빠지게 처리.
- 결과적으로 앱 번들이 **17.1MB → 9.55MB**로 줄었다. (사실 "넣지 말았어야 할 게 들어가 있던" 케이스라 거창한 건 아니고, 번들을 한 번 봤더니 보였던 것)
- 웹(Next.js)은 같은 목적을 `next/dynamic`ㅁ으로 처리.

### 3) lodash import 정리

- `import _ from 'lodash'`처럼 전체가 끌려오는 import가 흩어져 있었고, **RN 기본 번들러 Metro가 트리쉐이킹을 안 한다**는 점을 확인.
- 해결책(번들러 교체 / babel-plugin-lodash / 직접 import 수정) 중 **리스크 낮고 바로 적용 가능한 개별 모듈 import**를 선택.
- 해당 import 기준 71.5KB(gzip 25.2KB) → 2.3KB(gzip 965B). 단발 수정에 그치지 않게 **ESLint import 정렬**로 재유입을 막음.

---

## 모노레포 공유 패키지 구조 분리 (구조 설계)

앱·웹·웹뷰가 코드를 복붙해서 쓰던 상태를, **공유 패키지로 분리해 단일 소스화**하는 작업을 주도했다.

- `nerget-utils`(포맷터·검증·웹뷰 유틸), `nerget-styles`, `nerget-hooks` 등을 **독립 패키지로 추출**하고 각 앱이 패키지를 참조하도록 의존성을 정리.
- 작업 중 **패키지 간 순환 의존**을 발견·제거하고, UI(atoms→molecules→organisms→templates) 계층과 서비스/훅/유틸의 의존 방향을 단방향으로 정돈.
- Pages(라우팅)와 Screens(UI 로직)를 분리해 **웹·웹뷰 간 화면 재사용**이 가능한 구조로 전환.

> **효과**: 동일 로직의 중복 구현 제거, 한 곳 수정이 전 플랫폼에 반영. 신규 기능이 공유 컴포넌트 위에서 빠르게 조립됨.
> 

---

## 재사용 가능한 결제/체크아웃 레이아웃 시스템

체크아웃 종류가 여러 개(개통·요금제 변경·부가/데이터 상품)였고 각자 따로 구현돼 중복이 심했다.

- 공통 `CheckoutLayout` 템플릿을 **조합형(props composition)** 으로 설계 — 회선 정보·상품·결제 수단·금액 영역을 부품화.
- 결제 전/후 상태를 `CheckoutLayout` / `CheckoutResultLayout`으로 분리.
- 결제 중 **중복 제출 방지 트랜잭션 래퍼 + 전역 로딩 게이트**로 더블 탭/중복 결제를 구조적으로 차단.

> **포인트**: 결제 같은 고위험 동작에서 "중복 결제"는 흔한 사고 지점인데, 이를 **개별 화면이 아니라 공통 레이어에서** 막았다.
> 

---

## WebView ↔ Native 메시지 브리지 프로토콜

하이브리드 구조에서 화면 전환·토스트·뒤로가기를 화면마다 따로 짜던 것을 **표준 메시지 규약**으로 통일.

- `navigate / push / pop / replace / reset` 등 네비게이션 액션을 메시지 프로토콜로 정의.
- 멀티스텝 결제처럼 **여러 화면을 한 번에 교체**해야 하는 케이스를 위해 `popAndStack` 액션을 추가(현재 화면 pop 후 N개 화면을 원자적으로 스택) — 스택 꼬임 문제 해결.
- Android 백버튼 오버라이드 시 제스처 충돌 방지, 타입 제네릭으로 데이터 전달 안전성 확보.

---

## 웹뷰 쿠키 인증 디버깅 — 라이브러리 네이티브 코드까지 파봄

하이브리드(앱 안 웹뷰) 구조에서 앱의 토큰을 웹뷰에 쿠키로 넘겨 인증을 이어가는데, **특정 환경에서 쿠키가 의도대로 안 먹는 이슈**가 있었다. 표면에서 값만 맞추는 대신 `react-native-cookies`의 동작을 직접 확인했다.

- 쿠키 셋팅(Promise)과 웹뷰 렌더 순서를 로그로 추적해 **"순차 코드인데 왜 타이밍이 맞는지"**(set이 webview onLoadStart보다 빠름)를 확인.
- `CookieManager.set`의 **Android(`android.webkit.CookieManager`) / iOS(`WKHTTPCookieStore` vs `NSHTTPCookieStorage`) 네이티브 구현을 직접 읽고**, iOS WebKit 저장소가 "마운트 이전엔 set이 false positive가 날 수 있다 / 리셋 없이 재set이 바로 안 먹는다"는 차이를 정리.
- 실제 수정(NER-95 핫픽스)은 **웹/웹뷰 도메인에 따라 쿠키 URL을 분기**(`isWebview` 플래그 도입)하는 것으로 마무리. 원인을 네이티브 레벨에서 이해한 뒤 가장 작은 변경으로 픽스.
- 분석 과정은 사내 FE 위키(「웹뷰 쿠키」)에 정리.

> 초기 쿠키 인증 도입 자체는 동료 작업이고, 내 기여는 **이슈 디버깅 + 도메인 분기 핫픽스**다. 포인트는 "버그를 표면에서 막지 않고 라이브러리 네이티브 구현까지 내려가 이해하려 한 태도".
> 

---

## 안정성 기여 (공동 작업, 기여 범위 명시)

본인인증 이후 분기를 처리하는 **공통 오케스트레이션 훅**(설계·골격은 동료가 담당)에서,

**잘못된 회선 생성·중복 진입을 막는 비즈니스 규칙 분기**를 담당했다.

- 회선 보유 수 한도 체크, 자급제 단말의 회선 수 체크 예외 분기, 본인인증 후 상태 세팅 위치 정정 등 **엣지 케이스 정합성** 위주.
- 검증 누락 시 잘못된 회선이 생성되거나 사용자가 막다른 경로로 들어가는 것을 방지.

> 이 항목은 **공동 작업 중 내 기여 범위만** 기술했다. 훅 전체 통합 설계는 내가 한 것이 아니다.
> 

---

## 품질/유지보수 관점에서 한 일

- **캐시 정합성**: React Query 쿼리 키 계층화로 회선 변경 시 정확한 무효화, 로그아웃 시 캐시 정리로 다음 사용자 stale 데이터 노출 방지.
- **상태 변경 후 동기화 정합성**: 요금제 변경 직후 홈탭 회선 목록 갱신 시, 전용 스토어(`useLineLoadingCheckStore`)로 **로딩 표시가 필요한 경우에만 인디케이터를 노출**하고 나머지는 백그라운드 refetch하도록 분리. 동시에 진행 중인 재조회를 가드해 **중복 호출도 차단**.
- **import 컨벤션 정착**: 절대 경로 import + 자동 정렬로 디렉터리 이동에 강한 구조.

---

## 한 줄 요약

> 화면을 찍어내는 개발이 아니라, **번들을 측정해서 줄이고(트리쉐이킹·동적 import·분석 환경 구축), 중복 코드를 공유 패키지로 구조화하고, 결제 같은 고위험 플로우를 공통 레이어에서 안전하게 만든** 프론트엔드 개발을 했다.
> 

# AI 상담 어시스턴트 · 멀티 제품 웹 클라이언트 — 프론트엔드 개발

> 실시간 AI 상담 지원 도구의 웹 클라이언트를 **여러 제품이 한 백엔드를 공유하는 모노레포** 구조로 개발.
> 
> 
> 화면 구현보다 **여러 제품이 공유하는 프론트 플랫폼(공통 패키지·아키텍처·개발도구)을 만들고, 그 위에서 실시간 스트리밍 도메인을 안정적으로 다루는 일**에 집중했다.
> 
> *(회사·제품·내부 식별자·세부 도메인 규칙은 의도적으로 일반화했다.)*
> 

---

## 기술 스택

| 영역 | 사용 기술 |
| --- | --- |
| 공통 | TypeScript, 모노레포(pnpm workspace), Feature-Sliced Design |
| 프레임워크 | Next.js (App Router), React |
| 상태/데이터 | Zustand 계열 slice store, React Query |
| AI/스트리밍 | Vercel AI SDK 기반 자체 패키지, 커스텀 스트림 프로토콜 파서, 스트리밍 마크다운 렌더링 |
| API 계약 | OpenAPI 코드젠 파이프라인, Zod |
| 실시간 | STOMP over WebSocket |
| 디자인 시스템 | 자체 디자인 토큰 변환 패키지, shadcn, Tailwind |
| 인증 | 쿠키 기반 세션, JWT refresh, SSO |
| 개발도구 | MSW 기반 자체 Mock DevTool(2세대 자체 구축) |

---

## 핵심으로 신경 쓴 것

여러 제품이 같은 백엔드를 호출하는 구조에서는 **API 타입·인증·AI 스트리밍 처리가 제품마다 복제되면 변경 비용이 폭증한다.**

그래서 **공통 패키지로 결합을 끊고, 백엔드가 미완성이어도 프론트가 독립적으로 개발할 수 있는 도구를 만들고, 실시간 스트리밍이라는 까다로운 도메인을 안정적으로 다루는 것**을 우선했다.

---

## 1. AI 채팅 스트리밍 SDK 자체 구현 (가장 깊게 설계한 부분)

Vercel AI SDK를 그대로 쓰면 백엔드가 보내는 **커스텀 스트림 프로토콜(툴콜·출처 등)** 을 다룰 수 없고, 메시지 타입이 제품마다 고정돼 확장이 어려웠다.

- AI SDK 유틸을 선언만 두던 것을 **실제 구현체로 대체**하고, 백엔드 커스텀 스트림을 파싱하는 레이어를 추가.
- `useChat`에서 메시지를 **제네릭 타입**으로 변환 — 제품별 메시지 스키마를 타입 파라미터로 주입 가능하게 설계.
- retry를 마지막 메시지 기준(SDK 표준)으로 정리하고 요청 구성을 단순화, finish/에러 핸들링 정비.

> **포인트**: "라이브러리로 안 되는 것"을 직접 만들었다. 제품마다 다른 메시지 스키마를 한 SDK로 수용하고, 백엔드 고유 프로토콜을 표준 훅 인터페이스 뒤로 숨겼다.
> 

---

## 2. 스트리밍 마크다운 렌더링 전환

기존 마크다운 렌더러는 **토큰 단위 스트리밍 중간 상태에서 렌더가 깨지고**, 지식 검색이 무한 대기할 수 있었다.

- 여러 제품에서 스트리밍 친화 렌더러로 일괄 전환해 **중간 상태 렌더 안정화**.
- 검색에 타임아웃을 도입해 **무한 대기 차단**.
- 마크다운 렌더 memoization·입력 상태 분리로 리렌더 비용 절감.

> **포인트**: 사용자가 실제로 겪는 문제(응답이 깨져 보임, 검색이 멈춤)를 렌더링 레이어에서 구조적으로 잡았다.
> 

---

## 3. 자체 Mock DevTool 구축 (개발 인프라 — 2세대 직접 설계)

백엔드가 미완성인 상태에서 프론트가 **독립적으로 개발·테스트·시연**하려면 런타임에 제어 가능한 목 환경이 필요했다. 1세대 도구를 직접 만들고, 이후 MSW 기반으로 재설계했다.

- **1세대**: 역할(상담사/리더/관리자) 전환, 디버거 페이지, mock 데이터 제어가 가능한 자체 DevTool을 구축해 시연·QA·역할별 검증에 사용.
- **2세대(MSW 기반)**: 핸들러를 LocalStorage에 저장해 **런타임에 목 응답을 켜고 끄도록** 재설계. feature flag로 디버거 노출 제어, 워커 구조 리팩토링으로 빌드 호환성 정리.

> **포인트**: "내 화면만" 만든 게 아니라 **팀 전체가 백엔드 의존 없이 개발할 수 있는 환경**을 만들었다. 2세대로 한 번 더 갈아엎으며 운영 경험을 반영했다.
> 

---

## 4. FSD(Feature-Sliced Design) 아키텍처 마이그레이션

기존 페이지 종속 구조라 **위젯·도메인 로직 재사용이 어렵고 의존성이 얽혀** 있었다.

- `shared`/`entities`/`features`/`widgets`/`pages` 레이어를 도입하고, **페이지 단위로 점진 마이그레이션**해 위험을 분산.
- 도메인 로직을 entity로 승격하고, 페이지에 박힌 섹션을 widget으로 분리하면서 **store 직접참조를 훅으로 대체**.

> **포인트**: 한 번에 갈아엎지 않고 페이지별 점진 전환으로 진행 — 운영 중인 서비스에서 구조를 바꾸는 현실적 방법.
> 

---

## 5. 공통 패키지 — OpenAPI 코드젠 / 디자인 토큰 자동화

여러 제품 앱이 동일 백엔드를 호출하는데 **API 타입을 앱마다 수기로 정의**하고, 디자인 토큰을 코드에 박아 일관성을 유지하기 어려웠다.

- 공통 패키지에 **OpenAPI 스펙 → 타입 자동 생성 파이프라인**을 구성하고, 생성 모델을 그대로 쓰지 않고 `entities`에서 앱 도메인 모델로 **재정의하는 2단 구조**로 백엔드 스펙 변동과 앱 모델을 분리.
- 디자인 토큰을 **JSON → TS로 변환하는 스크립트**를 만들고 CI로 토큰 갱신을 자동화.

> **포인트**: 핵심은 자동 생성 코드의 양이 아니라, **스펙 변경을 한 번의 코드젠으로 전 앱에 전파**하고 **백엔드 모델과 앱 모델 사이에 매핑 경계를 둔** 설계 판단이다.
> 

---

## 6. 인증 세션 & 실시간 안정성 (기여)

- **인증 세션**: 401 시 단일 refresh로 토큰을 갱신하는 공통 처리, 중복 refresh를 단일 요청으로 통합, 다양한 진입 경로(포털·SSO)의 자동 로그인 대응.
- **실시간 WebSocket**: STOMP 구독 생명주기 안정화(구독 전 연결/권한 체크, 이전 토픽 unsubscribe, 중복 클라이언트 정리) — 실시간 데이터 누락 방지.
- **관측**: 상담 흐름에 로그를 심어 운영 중 추적 가능하게.

---

## 그 외

롤 기반 접근 제어, 모델 모드(Auto/Thinking) UI, 실시간 STT·품질평가 화면 등 도메인 기능을 다수 구현했다. 이 항목들은 위의 플랫폼/구조 작업만큼 설계 비중이 크진 않아 묶어 둔다.

---

## 한 줄 요약

> 화면을 찍어내는 개발이 아니라, **여러 제품이 공유하는 프론트 플랫폼(AI 스트리밍 SDK·공통 패키지·Mock DevTool·FSD 구조)을 만들고**, 그 위에서 실시간 스트리밍 상담 도메인을 **렌더 안정성·세션·WebSocket 정합성** 관점에서 다룬 프론트엔드 개발을 했다.
> 

> 통합 요약은 개발포폴-압축요약 참고.
> 

# 개발 포트폴리오 — 압축 요약

> AI 고객상담 지원 플랫폼을 **프론트엔드 · 백엔드 · 공통 코어** 세 레이어에 걸쳐 개발.
> 
> 
> 공통적으로 **"기능을 찍어내기보다, 여러 제품/서버가 공유하는 구조와 안전한 전환 방식을 먼저 설계"** 하는 일을 했다.
> 
> *(회사·제품·내부 식별자는 의도적으로 일반화했다.)*
> 

---

## 한눈에

| 레이어 | 한 일 | 대표 작업 |
| --- | --- | --- |
| **프론트엔드** | 여러 제품이 공유하는 프론트 플랫폼 구축 | AI 스트리밍 SDK 자체 구현, Mock DevTool, FSD 마이그레이션 |
| **백엔드 / AI** | 무중단 점진 전환 가능한 LLM 에이전트 서버 | 두 LLM 백엔드 트래픽 라우팅, feature flag 롤아웃, Enum Registry 패턴 |
| **공통 코어** | 여러 서버가 갈아끼울 수 있는 확장점 설계 | DI 컨테이너 다형성, LLM 백엔드 추상화, 안전망 동반 업그레이드 |

---

## 프론트엔드 — AI 상담 어시스턴트 멀티제품 클라이언트

- **AI 스트리밍 SDK 자체 구현** — Vercel AI SDK로 안 되는 백엔드 커스텀 프로토콜을 파싱하고, 제품별 메시지 스키마를 제네릭 타입으로 주입 가능하게 설계.
- **Mock DevTool 직접 구축(2세대)** — 백엔드 의존 없이 팀 전체가 개발·시연할 수 있는 MSW 기반 런타임 목 환경.
- **FSD 아키텍처 점진 마이그레이션** — 운영 중 서비스를 페이지 단위로 안전하게 구조 전환.
- **공통 패키지** — OpenAPI 코드젠·디자인 토큰 자동화로 스펙/토큰 변경을 전 앱에 전파, 백엔드/앱 모델 매핑 경계 설계.
- **실시간 안정성** — 스트리밍 렌더 깨짐·검색 무한대기 제거, WebSocket 구독 생명주기 정합성.

→ 상세: AI상담-어시스턴트-프론트엔드-개발포폴

---

## 백엔드 / AI — LLM 에이전트 상담 서버

- **두 LLM 백엔드 트래픽 라우팅** — 해시 버킷팅 + 런타임 비율로 클라우드↔On-Prem을 배포 없이 무중단 점진 전환(카나리), 사용자 단위 일관성 + graceful fallback.
- **feature flag 기반 신규 에이전트 롤아웃** — 기존 흐름 무수정, 미등록 시 기존 경로로 fallback, 재사용 가능한 에이전트 컴파일러.
- **코어/테넌트 경계 설계** — 공통 코어 패키지 분리, 설정 base/tenant 분리.
- **추상 Enum Registry 패턴 + 설계 문서화** — 코어의 테넌트 비의존성을 지키며 상수 확장.

→ 상세: AI상담-서버-백엔드-개발포폴

---

## 공통 코어 — 멀티 서버 공유 라이브러리

- **DI 컨테이너 다형성** — 테넌트가 "클래스 / 팩토리 / DI 컨테이너" 중 무엇으로든 구현을 끼울 수 있도록, 코어를 안 건드리는 확장점 설계.
- **LLM / Rerank 백엔드 추상화** — provider 분기로 백엔드 전환을 설정 문제로 축소, 응답 포맷 차이를 코어가 흡수.
- **동적 Enum의 JSON Schema 지원** — 멀티테넌트 동적성과 정적 스키마 노출을 양립.
- **안전망 동반 메이저 업그레이드** — baseline 테스트·점검 스크립트·가이드 문서와 함께 진행.

→ 상세: AI상담-공통코어-라이브러리-개발포폴

---

## 일관된 강점 (세 레이어 공통)

1. **구조 우선** — 기능을 넣기 전에 공유 가능한 경계·패턴부터 설계한다.
2. **무중단 전환** — feature flag·런타임 설정·점진 마이그레이션으로 위험을 분산한다.
3. **안전성 기본값** — graceful fallback과 하위호환을 일관되게 깔고 간다.
4. **도구를 만든다** — 팀이 백엔드 의존 없이 일할 수 있는 개발 환경(DevTool)을 직접 구축한다.
5. **문서로 의도를 남긴다** — 설계 패턴·업그레이드 가이드를 문서화해 팀이 따라 쓸 수 있게 한다.